{
  "name": "bbc-repo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install:all": "npm --prefix frontend install && npm --prefix infra install",
    "postinstall": "lefthook install && npm run install:all",
    "dev": "npm run install:all && npm --prefix frontend run dev"
  },
  "devDependencies": {
    "lefthook": "^1.6.15"
  }
}
